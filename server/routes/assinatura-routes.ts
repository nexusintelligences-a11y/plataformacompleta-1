import type { Express } from "express";

export function registerAssinaturaRoutes(app: Express): void {
  console.log("✅ Plataforma de Assinatura Digital importada com sucesso");
  // Rotas serão ativadas após revisão de imports
}
